# Marvel Comics SPA

SPA для просмотра персонажей и комиксов вселенной Marvel. Приложение получает данные через публичное Marvel API (через учебный прокси-сервер). Реализованы страницы со списками персонажей и комиксов, детальная страница комикса, случайный персонаж каждую минуту на главной странице.

## ✨ Особенности
- **Главная страница** — случайный персонаж каждую минуту, список персонажей с подгрузкой новых, информация о выбранном персонаже и его комиксах.
- **Страница комиксов** — список комиксов с подгрузкой новых, при клике — детальная страница комикса.
- **Кастомный хук useHttp** — управление состояниями загрузки и ошибок при HTTP-запросах.
- **Сервис MarvelService** — инкапсуляция логики работы с API.
- **Маршрутизация** — React Router (страницы: главная, комиксы, детальная комикса, 404).
- **Обработка ошибок** — ErrorBoundary, отображение сообщений об ошибках, спиннеры и скелетоны.
- **Lazy loading** — динамический импорт страниц для оптимизации загрузки.

## 🛠 Технологии
- **React** (функциональные компоненты, хуки)
- **React Router** (v6)
- **SCSS**
- **PropTypes**
- **Create React App**
- **Marvel API** (через учебный прокси-сервер)

## 🚀 Быстрый старт
1. **Клонируйте репозиторий**

```bash
git clone https://github.com/Sw1ftFox/marvel-comics-spa.git
cd marvel-comics-spa
```

2. **Установите зависимости**

```bash
npm install
```

3. **Запустите приложение**

```bash
npm start
```

Приложение будет доступно по адресу `http://localhost:3000`.
*Примечание*: в `package.json` настроен прокси `"proxy": "https://marvel-server-zeta.vercel.app/"` — запросы к API будут автоматически перенаправляться, чтобы избежать CORS.

## 📁 Структура проекта
```text
src/
├── components/          # Компоненты (app, randomChar, charList, charInfo, comicsList, pages, etc.)
├── hooks/               # Кастомные хуки (http.hook.js)
├── services/            # Работа с API (MarvelService.js)
├── style/               # SCSS-стили (переменные, общие стили, кнопки)
├── resources/           # Изображения
├── index.js             # Точка входа
└── ...
```

## 🧩 Основные функции
- Случайный персонаж на главной обновляется по кнопке и раз в минуту.
- Список персонажей подгружается порциями (по 9 персонажей), при клике на персонажа отображается его информация и список комиксов с ним.
- Список комиксов с подгрузкой новых, по клику открывается страница с деталями комикса (описание, цена).
- 404 страница для несуществующих маршрутов.
- Спиннеры и скелетоны во время загрузки данных.

Демо: [Ссылка на хостинг](https://marvel-comics-spa.vercel.app/)

Репозиторий: [Ссылка на GitHub](https://github.com/Sw1ftFox/marvel-comics-spa)